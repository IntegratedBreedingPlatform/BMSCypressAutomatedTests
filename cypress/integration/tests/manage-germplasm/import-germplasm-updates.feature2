@import-germplasm-updates
@manage-germplasm
Feature: Import Germplasm Updates
    In order to update existing germplasm records in BMS
	As a user
	I want to import updates of germplasm records using a template file

@TestCaseKey=IBP-T2177    
@sanity-test
@clean-install
    Scenario: Import germplasm updates using basic template
        Given I am on the Manage Germplasm page of specified program
        When I select Import germplasm updates from Actions
        And I select a file to upload
        Then saving of germplasm updates succeeds
@TestCaseKey=IBP-T2177    
@smoke-test
    Scenario: Import germplasm updates using a template
        Given I am on the Manage Germplasm updates page of specified program
        When I select Import germplasm from Actions
        And I select a file to upload
        Then saving of germplasm updates succeeds
#Note: The scenarios above are duplicated to be able to include import germplasm updates
# when running TAGS=(@smoke-test) and not @ignore and not @clean-install